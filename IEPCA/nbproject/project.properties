build.classes.dir=${build.dir}/jar
build.dir=build
build.generated.dir=${build.dir}/generated
com.sun.jbi.routing=true
com.sun.jbi.routing.bc.autoconnect=true
com.sun.jbi.sa.internal.routing=true
dist.dir=dist
dist.jar=${dist.dir}/IEPCA.zip
jbi.content.additional=${reference.IEPSystem.dist_se}
jbi.content.component=sun-iep-engine
jbi.service-assembly.description=Represents the Service Assembly of IEPCA
jbi.service-assembly.id=IEPCA
jbi.service-unit.description=Represents this Service Unit
meta.inf=${source.root}/conf
project.IEPSystem=../IEPSystem
reference.IEPSystem.dist_se=${project.IEPSystem}/build/SEDeployment.jar
resource.IEPSystem=${project.IEPSystem}/setup
source.encoding=UTF-8
source.root=src
src.build.dir=${source.root}/jbiServiceUnits
src.dir=${source.root}/jbiasa
test.dir=test
test.results.dir=${basedir}/test/results
